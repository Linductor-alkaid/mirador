# src/geometry

`mirador::geometry` 模块实现目录：`LineDetector` SPI、几何过滤与 ELSED 等传统视觉适配
（可选依赖，许可证审查先行）。内容随 M3 里程碑落地；M6（实验轨道）追加
`geometric_proposal.cpp`——闭合/近闭合线段结构的区域 proposal 分析（issue #11 假设
验证，`DEC-017`，API 非冻结）。
